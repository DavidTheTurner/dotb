import React from "react";
import { Screen } from "./Components/Screen";

const App: React.FC = () => {
  return (
    <React.StrictMode>
      <Screen />
    </React.StrictMode>
  );
};

export default App;
